# C语言——数组与指针

## 一、数组

数组就是存放多个相同类型数据的一种方式。

### 1. 数组定义

数组下标从 0 开始。

例如一个有 5 个元素的数组，下标就是 0、1、2、3、4。

### 2. 数组初始化

数组可以在定义的时候直接赋值。

例如：int a[5] = {1, 2, 3, 4, 5};

也可以不写数组大小，让系统根据数据个数自动确定。

### 3. 数组的使用

可以通过数组下标访问元素。

例如 a[0] 表示第一个元素，a[2] 表示第三个元素。

数组经常和 for 循环一起使用，可以依次访问数组中的所有元素。

## 二、指针

指针主要用来存放变量的地址。

### 1. 指针定义

例如：

int a = 10;
int *p = &a;

这里 &a 表示 a 的地址，p 保存 a 的地址，*p 表示 a 的值。

### 2. 通过指针修改数据

如果 p 指向 a，那么修改 *p 的值，也就是修改 a 的值。

## 三、数组和指针

数组名在很多情况下可以看作数组第一个元素的地址。

例如定义一个数组后，可以让一个指针指向数组的第一个元素。

指针使用 ++ 后，可以指向数组的下一个元素。

数组和指针的关系：

a[i] 和 *(a + i) 都可以表示数组的第 i 个元素。

## 总结

数组：用来存放多个相同类型的数据。

指针：用来存放地址。

&：取地址。

*：取指针指向的值。


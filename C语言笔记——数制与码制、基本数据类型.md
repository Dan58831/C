## 一、数制与码制

### 1. 数制

数制就是表示数字的方法，常见的有：

二进制

八进制

十进制

十六进制

### 2. 十进制

0 - 9

### 3. 二进制

1011

转换成十进制：

1×2³ + 0×2² + 1×2¹ + 1×2⁰
= 8 + 0 + 2 + 1
= 11

所以：(1011)₂ = (11)₁₀

### 4. 八进制

0 - 7

转换成十进制：eg

1×8¹ + 7×8⁰
= 8 + 7
= 15

### 5. 十六进制

十六进制有 16 个符号：

0 - 9   A - F

1A转换成十进制：1×16¹ + A×16⁰= 16 + 10

### 6. C语言中的进制

在 C 语言中，整数可以使用不同的进制表示。

int a = 10;    十进制
int b = 012;     八进制
int c = 0xA;    十六进制

十进制：直接写
八进制：数字前面加 0
十六进制：数字前面加 0x

### 7. 原码

原码就是用最高位表示符号。

0 正数
1 负数

最高位是符号位，剩下的位表示数值。

### 8. 反码

正数的反码和原码一样。

负数的反码就是：

**符号位不变，其余各位取反。**

例如：

+5原码：00000101

-5原码：10000101
-5反码：11111010

### 9. 补码

正数的补码和原码一样。

负数的补码：反码 + 1

例如：

-5原码：10000101
-5反码：11111010
-5补码：11111011

计算机中整数通常使用补码来表示和进行运算。

补码的一个好处是可以把加法和减法统一处理

## 二、基本数据类型

C语言中常见的基本数据类型有：

char
int
float
double

void

### 1. char 字符型

char用来存储字符。

例如：char ch = 'A';

字符使用单引号：

'A'
'b'
'1'

字符串则使用双引号：

"A"

例如：

```c
#include <stdio.h>

int main()
{
    char ch = 'A';

    printf("%c\n", ch);

    return 0;
}
```

输出：

A

char 通常占 1 个字节。

字符实际上也对应一个整数编码，所以也可以使用 `%d` 输出它对应的数值。

char ch = 'A';
printf("%c\n", ch);
printf("%d\n", ch);

### 2. int 整型

int 用来存储整数。

例如：

int a = 10;
int b = -20;

输出整数一般使用 %d：

printf("%d\n", a);

例如：

```c
#include <stdio.h>

int main()
{
    int a = 100;

    printf("%d\n", a);

    return 0;
}
```

### 3. float 单精度浮点型

float 用来存储小数。

例如：

float a = 3.14f;

后面的 f 表示这个数是 float 类型。

输出可以使用：

printf("%f\n", a);

默认情况下 %f 会显示 6 位小数。

例如：

float a = 3.14f;

printf("%f\n", a);

结果3.140000

如果只想显示两位小数，可以写：

printf("%.2f\n", a);

### 4. double 双精度浮点型

double 也用来存储小数，一般来说它的精度比 float高。

例如：

double pi = 3.1415926535;

在 printf 中可以使用 %f 输出：

printf("%f\n", pi);

如果对小数精度要求比较高，一般会使用 double。

### 5. void

void 表示没有具体的数据类型。

比较常见的是用在函数中。

例如：

```c
void test()
{
    printf("Hello");
}
```

函数没有返回值

### 6. sizeof

sizeof 可以用来计算一个数据类型或者变量占用多少字节。

例如：

```c
#include <stdio.h>

int main()
{
    printf("%zu\n", sizeof(char));
    printf("%zu\n", sizeof(int));
    printf("%zu\n", sizeof(float));
    printf("%zu\n", sizeof(double));

    return 0;
}
```

比较常见的情况是：

char      1 字节
int       4 字节
float     4 字节
double    8 字节

但是具体大小和使用的平台、编译器有关，所以不能认为所有情况下都完全一样。

### 7. 数据类型转换

不同类型的数据一起运算时，可能会发生类型转换。

例如：

int a = 10;
double b = 3.14;

double c = a + b;

 a 会在运算过程中进行类型转换。

也可以自己进行强制类型转换。

格式：

(类型)变量

例如：

```c
int a = 10;
int b = 3;

double c = (double)a / b;
```

如果直接写：

```c
double c = a / b;
```

因为 a 和 b 都是 int，所以会先进行整数除法：

```text
10 / 3 = 3
```

而会进行浮点数除法：

```c
(double)a / b
```

```text
10 / 3 ≈ 3.333333
```

所以做除法的时候要特别注意数据类型
